export const canEditFacultyFields = (role) => role === "faculty";

export const canEditFacultyScore = (role) => role === "faculty";
export const canEditHodScore = (role) => role === "hod";
export const canEditDeanScore = (role) => role === "director";